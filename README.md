# dinolingo-backend

For now, its goal is to keep slack app client ID secure.

## How to run

```bash
    bun install
    bun run index.js
```

## Setup

You need to export 3 env variables:
- SLACK_CLIENT_ID
- SLACK_CLIENT_SECRET
- SLACK_REDIRECT_URI
- DINOPORT
